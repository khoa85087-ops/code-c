//in ra mảng tăng dần  lớn nhất  trong 1 cái mảng có nhiều mảng con tăng dần
//tính tổng trong mảng con lớn nhất,số phần tử 
// mang a[n]:a[0] tới a[n-1]
//code thoát ở else để j được tăng đúng--> duyệt in dễ 

#include "stdio.h"
int max(int a[],int n);
int main (){
	int n;int i;int j;
	back:
	printf("nhap vao so nguyen n:");
	scanf ("%d",&n);
	if (n<=0||n>100) goto back;
	int a[n+1]; int sum[n];int lent[n];
	for (i=0;i<n;i++){
		printf ("a[%d]:",i);
		scanf ("%d",&a[i]);
	}

	j=0;
	sum[j]=0;lent[j]=0;
	a[n]=a[n-1]-1;
	for (i=0;i<n;i++){
		if (a[i+1]>a[i]){
			sum[j]=sum[j]+a[i];
			lent[j]++;
		}
		else {
			sum[j]=sum[j]+a[i];
			printf ("\n  %d   \n",sum[j]);
			lent[j]++;
			j++;
			sum[j]=0;lent[j]=0;
		}
	}

	int max_sum=max(sum,j);
	for (i=0;i<j;i++)
	{
		if (sum[i]==max_sum){
			printf ("do la la :%d",lent[i]);
			printf ("\ntong la :%d",max_sum);
		}
	}
	
}
int max(int a[],int n){
	int i;
	int max_ar=a[0];
	for (i=1;i<n;i++){
		if (a[i]>max_ar){
			max_ar=a[i];
		}
	}
	return max_ar;
}
